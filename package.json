{
  "name": "@nebuly-ai/nebuly-js-sdk",
  "version": "0.1.4",
  "description": "Nebuly-AI server side SDK for JavaScript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc && tsc -p tsconfig.amd.json && tsc -p tsconfig.esm.json",
    "publish": "npm run build && npm publish --access public"
  },
  "dependencies": {
    "@langchain/core": "^0.1.21",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.16"
  }
}
